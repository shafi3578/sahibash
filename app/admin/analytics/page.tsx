import { requirePermission } from "@/lib/auth";
import { getMarketplaceSnapshot } from "@/lib/analytics/marketplace";

export default async function MarketplaceAnalyticsPage(){
 await requirePermission("search.view"); const s=await getMarketplaceSnapshot();
 const cards=[["Active quality listings",s.activeListings],["Meaningful contacts · 7 days",s.meaningfulContacts],["Listings contacted · 7 days",s.contactedListings],["Searches · 7 days",s.searches],["Zero-result searches",s.zeroResults],["Listing contact coverage",`${(s.contactRate*100).toFixed(1)}%`]];
 return <main className="mx-auto w-full max-w-7xl px-4 py-8 sm:px-6 lg:px-8"><p className="text-xs font-bold uppercase tracking-widest text-[var(--accent)]">Marketplace north star</p><h1 className="font-display text-3xl font-bold">Liquidity & operations</h1><p className="mt-2 text-sm text-[var(--ink-2)]">Real buyer–seller connections and supply gaps. No fabricated activity.</p><div className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{cards.map(([label,value])=><section key={label} className="rounded-2xl border border-[var(--line)] bg-white p-5"><p className="text-sm text-[var(--ink-2)]">{label}</p><p className="mt-2 text-3xl font-bold">{value}</p></section>)}</div><section className="mt-6 rounded-2xl border border-[var(--line)] bg-white p-5"><h2 className="font-display text-xl font-bold">Supply opportunities</h2><p className="mt-1 text-sm text-[var(--ink-2)]">Repeated searches with no genuine results, ordered by demand.</p><div className="mt-4 divide-y divide-[var(--line)]">{s.topOpportunities.length?s.topOpportunities.map(x=><div key={x.term} className="flex justify-between py-3"><span>{x.term}</span><strong>{x.count}</strong></div>):<p className="py-4 text-sm text-[var(--ink-2)]">No repeated zero-result opportunities in the last seven days.</p>}</div></section></main>;
}
