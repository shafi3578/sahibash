"use server";
import {revalidatePath} from "next/cache"; import {requireUser} from "@/lib/auth"; import {createSupabaseServerClient} from "@/lib/supabase/server";
export async function saveNotificationPreferencesAction(formData:FormData){const user=await requireUser();const supabase=await createSupabaseServerClient();const yes=(k:string)=>formData.get(k)==="on";const locale=String(formData.get("locale")??"fa");const {error}=await supabase.from("notification_preferences").upsert({user_id:user.id,locale:["en","fa","ps"].includes(locale)?locale:"fa",new_messages:yes("new_messages"),listing_moderation:yes("listing_moderation"),listing_expiry:yes("listing_expiry"),saved_search_matches:yes("saved_search_matches"),saved_listing_changes:yes("saved_listing_changes"),updated_at:new Date().toISOString()},{onConflict:"user_id"});if(error)throw new Error("Unable to save notification preferences.");revalidatePath("/dashboard/settings");}
